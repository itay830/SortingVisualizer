#include "../include/SortingMethod.h"

SortingMethod::SortingMethod(const std::vector<int> &arr)
	: arr(arr){

}

void SortingMethod::draw(const raylib::Window &window) {
	// TODO: Add the default drawing method
}
